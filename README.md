# 🚀 Template Next.js + Tailwind CSS

Um template limpo e funcional para iniciar projetos rapidamente.

## 📦 O que está incluído
- Next.js 14 (App Router)
- Tailwind CSS v3 configurado
- TypeScript pronto
- Estrutura básica de pastas

## ⚡ Começando

1.  **Baixe o template:**
    - Clique no botão verde `<> Code` acima.
    - Selecione `Download ZIP`.
    - Extraia a pasta onde quiser.

2.  **Instale as dependências:**
    Abra o terminal na pasta do projeto e execute:
    ```bash
    npm install
    ```

3.  **Execute o servidor de desenvolvimento:**
    ```bash
    npm run dev
    ```
    Abra [http://localhost:3000](http://localhost:3000) no navegador para ver o site.

## 🗂️ Estrutura do Projeto
